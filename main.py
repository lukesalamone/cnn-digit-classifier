from run_model import RunModel as run






if __name__ == "__main__":
    print('Welcome to the interactive digit classifier. We will use a convolutional neural network to classify handwritten digits into one of 10 categories.')

    epochs = input('Number of epochs to train for (default 100): ')
